import 'package:flutter/material.dart';

class CreateNewLableScreen extends StatelessWidget {
  const CreateNewLableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Create New Lable Screer"),
    );
  }
}
